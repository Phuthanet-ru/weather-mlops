# 🔹 ชื่อของ Workflow
name: MLOps Pipeline CI/CT/CD

# 🔹 Trigger: เมื่อมีการ push หรือ pull request ไปยัง branch main
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  # -------------------- 🔹 JOB 1: CI Checks --------------------
  ci-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install CI dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r mlops_pipeline/requirements.txt
          pip install flake8 pytest

      - name: 🔎 Lint with flake8
        run: |
          flake8 deployment mlops_pipeline/scripts --count --show-source --statistics
          flake8 deployment mlops_pipeline/scripts --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
          echo "✅ Flake8 check passed."

      - name: 🧪 Test with pytest
        run: |
          pytest || (if [ $? -eq 5 ]; then echo "No tests found. Skipping."; exit 0; else exit $?; fi)
          echo "✅ Unit tests passed."


  # -------------------- 🔹 JOB 2: Train Models (CT) --------------------
  train-and-compare:
    needs: ci-checks
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        experiment:
          - lr: 0.001
            epochs: 10
            run_name: 'base-config'
          - lr: 0.0005
            epochs: 10
            run_name: 'low-lr'
          - lr: 0.001
            epochs: 20
            run_name: 'high-epochs'
    env:
      MLFLOW_TRACKING_URI: ${{ secrets.MLFLOW_TRACKING_URI }}
      MLFLOW_TRACKING_USERNAME: ${{ secrets.MLFLOW_TRACKING_USERNAME }}
      MLFLOW_TRACKING_PASSWORD: ${{ secrets.MLFLOW_TRACKING_PASSWORD }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r mlops_pipeline/requirements.txt
          pip install kaggle tensorflow mlflow pillow numpy

      - name: Download Dataset and Fix Structure
        env:
          KAGGLE_JSON: ${{ secrets.KAGGLE_JSON }}
        run: |
          echo "$KAGGLE_JSON" > kaggle.json
          mkdir -p ~/.kaggle
          mv kaggle.json ~/.kaggle/
          chmod 600 ~/.kaggle/kaggle.json
          mkdir -p mlops_pipeline/data
          kaggle datasets download -d ammaralfaifi/5class-weather-status-image-classification -p mlops_pipeline/data --unzip
          if [ -d "mlops_pipeline/data/data" ]; then
            mv mlops_pipeline/data/data/* mlops_pipeline/data/
            rm -rf mlops_pipeline/data/data
          fi
          echo "✅ Dataset prepared."

      - name: Create safe temporary directory
        run: |
          # สร้างโฟลเดอร์ชั่วคราวที่ใช้โดย TMPDIR
          mkdir -p ${{ github.workspace }}/tmp
          echo "✅ Created safe temporary directory for MLflow artifacts."

      - name: 💣 Force cleanup of non-image files via Shell
        run: |
          DATA_PATH="mlops_pipeline/data"
          echo "🔎 Forcing cleanup of non-image files in ${DATA_PATH}..."

          # ค้นหาไฟล์ทั้งหมดที่ 'นามสกุล' ไม่ใช่ไฟล์ภาพที่ TensorFlow รองรับ 
          # และลบไฟล์เหล่านั้น
          find ${DATA_PATH} -type f \
              ! -name "*.jpg" \
              ! -name "*.jpeg" \
              ! -name "*.png" \
              ! -name "*.gif" \
              ! -name "*.bmp" \
              ! -name "*.webp" \
              ! -name "*." \
              -delete

          echo "✅ Aggressive file cleanup completed. Any remaining files are either corrupt images or files without common extensions."

      - name: 🔍 Check and remove corrupted images
        run: |
          python mlops_pipeline/scripts/find_corrupted.py
          echo "✅ Corrupted image check completed."
  
      - name: "Run Model Training (Exp: ${{ matrix.experiment.run_name }})"
        env:
            # 🚨 บังคับใช้พาธชั่วคราวที่ปลอดภัยสำหรับ Python/MLflow/OS
          TMPDIR: ${{ github.workspace }}/tmp
          TEMP: ${{ github.workspace }}/tmp
          MP: ${{ github.workspace }}/tmp
        run: |
          # 1. สร้างโฟลเดอร์ชั่วคราวที่ปลอดภัยก่อนรัน (เผื่อกรณีที่ mkdir -p ยังไม่พอ)
          mkdir -p ${{ github.workspace }}/tmp
          echo "✅ Created safe temporary directory: ${{ github.workspace }}/tmp"
  
          # 2. รันการเทรน
          echo "🚀 Running Exp: LR=${{ matrix.experiment.lr }}, Epochs=${{ matrix.experiment.epochs }}"
          python mlops_pipeline/scripts/03_train_model.py \
              --lr ${{ matrix.experiment.lr }} \
              --epochs ${{ matrix.experiment.epochs }}

      - name: Inspect dataset (for debugging)
        run: |
          echo "📁 Dataset files preview:"
          find mlops_pipeline/data -type f | head -20

      - name: Upload MLflow Artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: mlflow-artifacts-${{ matrix.experiment.run_name }}
          path: mlruns/


  # -------------------- 🔹 JOB 3: Deploy and Test (CD) --------------------
  deploy-and-test:
    needs: train-and-compare
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          pip install mlflow pillow

      - name: Download MLflow artifacts
        uses: actions/download-artifact@v4
        with:
          path: .

      - name: Consolidate MLflow artifacts
        run: |
          # 1. สร้างโฟลเดอร์ปลายทาง
          mkdir -p mlruns
          
          # 2. วนลูปผ่านโฟลเดอร์ Artifacts ที่ถูกดาวน์โหลด
          for dir in mlflow-artifacts-*; do
            echo "Consolidating artifacts from $dir"
            # คัดลอกเนื้อหาทั้งหมดในโฟลเดอร์ artifact (ซึ่งคือ EXP_ID) 
            # ไปยัง mlruns/
            # ใช้ find เพื่อคัดลอกเฉพาะโฟลเดอร์ EXP_ID และ RUN_ID
            find "$dir" -maxdepth 1 -mindepth 1 -type d -exec cp -r {} mlruns/ \;
          done
          echo "✅ Consolidated mlruns directory."
          # ตรวจสอบว่าไฟล์ MLmodel อยู่ในโครงสร้างที่คาดหวังหรือไม่
          find mlruns -type f -name "MLmodel" | head -10

      - name: Copy latest model for deployment
        run: |
          echo "📦 Searching for trained model..."
          # 1. ค้นหาไฟล์ MLmodel ทั้งหมด
          MODEL_FILES=$(find mlruns -type f -name "MLmodel")

          if [ -z "$MODEL_FILES" ]; then
            echo "❌ No MLmodel file found within mlruns/ directory structure."
            exit 1
          fi

          # 2. คัดเลือกโฟลเดอร์โมเดลที่ถูกสร้างขึ้นล่าสุด (โดยใช้ find และ sort ตามเวลา)
          # เราใช้ ls -td (เรียงตามเวลา) บนไดเรกทอรีของไฟล์ MLmodel
          LATEST_MODEL_DIR=$(echo "$MODEL_FILES" | xargs dirname | xargs ls -td | head -1)

          if [ -z "$LATEST_MODEL_DIR" ]; then
            echo "❌ Failed to determine the latest model directory."
            exit 1
          fi

          # 3. ดำเนินการคัดลอก
          mkdir -p model
          cp -r "$LATEST_MODEL_DIR"/* model/
          echo "✅ Copied model from $LATEST_MODEL_DIR"
          ls -R model

      - name: Build Docker Image
        run: docker build -t weather-mlops-api .

      - name: Run Docker Container
        run: |
          docker run -d -p 8001:8000 --name weather_api weather-mlops-api
          echo "⏳ Waiting for container (20s)..."
          sleep 20
          docker ps

      - name: Test API Prediction
        run: |
          IMAGE_PATH="test_assets/1440909480.jpg"
          echo "🧪 Testing API Prediction..."
          curl --fail --retry 5 --retry-delay 5 http://127.0.0.1:8001/
          curl -X POST -F "file=@${IMAGE_PATH}" http://127.0.0.1:8001/predict --fail
          echo "✅ API test passed."

      - name: Stop and Remove Docker Container
        if: always()
        run: |
          docker stop weather_api || true
          docker rm weather_api || true
          echo "🧹 Cleaned up container"
